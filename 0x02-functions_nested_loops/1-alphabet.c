#include "main.h"

/**
 * Print the alphabet in lower case
 * return: void
 */

void print_alphabet(void)
{
	int alpha;

	for (alpa = 'a'; aplha <= 'z'; alpha++)
		_putchar(alpha);

	_putchar('\n')
}
